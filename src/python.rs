use crate::println;

pub fn exec(input: [usize; 255]) {
    let _tokenized_code = tokenize(input);
}

fn tokenize(input: [usize; 255]) -> [i8; 255] {
    let tokens: [i8; 255] = [0; 255];

    println!("tokenizing code");

    tokens
}